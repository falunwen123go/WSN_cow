## 🚀 项目运行指南

### 一、环境准备

**必需软件**：

- JDK 8+ (后端)
- MySQL 8.0+ (数据库)
- Node.js 16+ (前端)
- Python 3.7+ (传感器模拟器)

---

### 二、数据库启动

**1. 启动MySQL服务**
```powershell
# 确保MySQL服务正在运行
net start mysql
```

**2. 确认数据库已创建**
- 数据库名：`wsn_cow`
- 表：7张（已在Day 1创建）

---

### 三、后端启动（Spring Boot）

**方式1：使用IDEA运行**
```powershell
# 1. 进入后端目录
cd d:\OneDrive\桌面\cursor\WSN_cow\backend

# 2. 在IDEA中打开backend文件夹
# 3. 找到 com.wsn.cow.WsnCowApplication
# 4. 右键 -> Run 'WsnCowApplication'
```

**方式2：使用Maven命令行**
```powershell
# 在backend目录下执行
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
cd d:\OneDrive\桌面\cursor\WSN_cow\backend
.\mvnw.cmd spring-boot:run
```

**验证后端启动成功**：
- 访问：http://localhost:9090
- 看到类似 "Whitelabel Error Page" 说明Spring Boot已启动
- 日志显示：`Started WsnCowApplication in X seconds`

**后端API列表**（共38个）：
- 传感器数据：`GET /api/sensor/latest`, `/api/sensor/history` 等
- 节点管理：`GET /api/node/list`, `/api/node/{id}` 等
- 报警信息：`GET /api/alarm/list`, `/api/alarm/handle` 等
- 设备控制：`POST /api/device/control`, `/api/device/mode/switch` 等

---

### 四、前端启动（Vue 3）

```powershell
# 1. 进入前端目录
cd d:\OneDrive\桌面\cursor\WSN_cow\frontend

# 2. 设置PowerShell执行策略（如果之前没设置）
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

# 3. 启动开发服务器
npm run dev
```

**前端访问**：
- 地址：http://localhost:5173/
- 默认页面：数据监控大屏（Dashboard）

**前端功能**：
- ✅ 6个页面路由可访问
- ✅ 侧边栏导航正常
- ⚠️ 页面内容为占位，Day 7将开发实际功能

---

### 五、传感器模拟器启动（可选）

```powershell
# 1. 进入模拟器目录
cd d:\OneDrive\桌面\cursor\WSN_cow\sensor_simulator

# 2. 运行Python脚本
python sensor_simulator.py
```

**模拟器功能**：
- 每30秒生成一次传感器数据
- 模拟3个节点（节点ID: 1001, 1002, 1003）
- 数据发送到：http://localhost:9090/api/sensor/data

---

### 六、完整启动顺序

**推荐按以下顺序启动**：

```powershell
# 终端1：启动后端
cd d:\OneDrive\桌面\cursor\WSN_cow\backend
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\mvnw.cmd spring-boot:run

# 等待后端启动完成（看到 "Started WsnCowApplication"）

# 终端2：启动前端
cd d:\OneDrive\桌面\cursor\WSN_cow\frontend
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
npm run dev

# 终端3（可选）：启动传感器模拟器
cd d:\OneDrive\桌面\cursor\WSN_cow\sensor_simulator
python sensor_simulator.py
```

---

### 七、验证系统运行

**1. 检查后端**
```powershell
# 测试API（在PowerShell中）
Invoke-WebRequest -Uri "http://localhost:9090/api/node/list" -Method GET
```

**2. 访问前端**
- 打开浏览器：http://localhost:5173/
- 点击侧边栏菜单，测试6个页面

**3. 查看数据库**
```sql
-- 查看传感器数据
SELECT * FROM sensor_data ORDER BY create_time DESC LIMIT 10;

-- 查看节点信息
SELECT * FROM node_info;

-- 查看报警信息
SELECT * FROM alarm_info ORDER BY alarm_time DESC LIMIT 10;
```

---

### 八、当前系统状态

**✅ 已完成（Day 1-6）**：
- Day 1-3: 数据库、后端基础、Python模拟器
- Day 4: 23个RESTful API
- Day 5: 15个设备控制API + 数据库修复
- Day 6: Vue 3前端初始化、布局、API封装、公共组件

**⏳ 待开发（Day 7-14）**：
- Day 7-8: 前端页面功能实现
- Day 9-10: 数据可视化（ECharts图表）
- Day 11-12: 系统集成测试
- Day 13-14: 优化和文档

---

### 九、常见问题

**Q1: 后端启动报错 "Address already in use"**
- **原因**：9090端口被占用
- **解决**：
  ```powershell
  # 查找占用端口的进程
  netstat -ano | findstr :9090
  # 杀死进程（替换<PID>为实际进程ID）
  taskkill /F /PID <PID>
  ```

**Q2: 前端npm命令报错**
- **原因**：PowerShell脚本执行策略
- **解决**：每次新开终端执行
  ```powershell
  Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
  ```

**Q3: 数据库连接失败**
- **检查**：MySQL服务是否启动
- **检查**：application.yml 中的数据库配置
  ```yaml
  spring:
    datasource:
      url: jdbc:mysql://localhost:3306/wsn_cow
      username: root
      password: 你的密码
  ```

**Q4: 前端API请求失败（跨域）**
- **无需担心**：Vite已配置proxy，自动代理到9090端口
- **确认后端已启动**

---

### 十、快速验证脚本

创建一个测试脚本，一键验证所有服务：

```powershell
# 保存为 check_services.ps1
Write-Host "=== 检查WSN项目服务状态 ===" -ForegroundColor Green

# 检查后端
Write-Host "`n1. 检查后端 (9090端口)..." -ForegroundColor Yellow
try {
    $response = Invoke-WebRequest -Uri "http://localhost:9090/api/node/list" -Method GET -TimeoutSec 3
    Write-Host "✓ 后端运行正常" -ForegroundColor Green
} catch {
    Write-Host "✗ 后端未启动或无响应" -ForegroundColor Red
}

# 检查前端
Write-Host "`n2. 检查前端 (5173端口)..." -ForegroundColor Yellow
try {
    $response = Invoke-WebRequest -Uri "http://localhost:5173" -Method GET -TimeoutSec 3
    Write-Host "✓ 前端运行正常" -ForegroundColor Green
} catch {
    Write-Host "✗ 前端未启动或无响应" -ForegroundColor Red
}

# 检查数据库
Write-Host "`n3. 检查MySQL服务..." -ForegroundColor Yellow
$mysqlService = Get-Service -Name "mysql*" -ErrorAction SilentlyContinue
if ($mysqlService -and $mysqlService.Status -eq "Running") {
    Write-Host "✓ MySQL服务运行中" -ForegroundColor Green
} else {
    Write-Host "✗ MySQL服务未运行" -ForegroundColor Red
}

Write-Host "`n=== 检查完成 ===" -ForegroundColor Green
```

运行：
```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\check_services.ps1
```

---

**总结**：目前项目后端和前端框架已搭建完成，可以正常启动运行。前端页面功能（数据展示、图表等）将在Day 7-8开发。现在启动后可以看到布局和导航，但具体业务页面还是占位状态。